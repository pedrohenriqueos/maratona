#include <bits/stdc++.h>
using namespace std;

int main(){
   int N;
   cin >> N;
   for(int x=0;x<N;x++){
   string exp;
   cin >> exp;
   stack<char> pilha;
   for(int i=0;i<exp.size();i++)
   pilha.push(exp[i]);

   }
   return 0;
}
