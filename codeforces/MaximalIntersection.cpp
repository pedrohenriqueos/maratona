#include<bits/stdc++.h>

using namespace std;

#define f first
#define s second
#define inf 0x3f3f3f3f
main(){
	int N;
	scanf("%lld",&N);
	vector< pair<int,int> > A(N);
	
	for(int i=0;i<N;i++)
		scanf("%lld %lld",&A[i].f,&A[i].s);
	printf("%lld\n",max(out,0LL));
}
